<Window
    x:Class="SysTrayNET.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="clr-namespace:SysTrayNET.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:SysTrayNET"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=WindowTitel, FallbackValue=WindowTitel}"
    Width="800"
    Height="450"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <Behaviors:NotifyTrayIconBehavior />
    </i:Interaction.Behaviors>

    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/Resources/Picture/ApplicationIcon.png" />
    </Window.Icon>

    <Grid x:Name="gridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24">
            <Run Text="WPF Window im Systray" />
            <LineBreak />
            <LineBreak />
            <Run Text="Ctrl-Alt F4 (Beenden)" />
            <LineBreak />
            <Run Text="Ctrl-Alt W (Dialog öffnen)" />
        </TextBlock>

    </Grid>
</Window>
